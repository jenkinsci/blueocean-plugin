export {
    default as extensionRegistry,
    ExtensionPoint,
    Extension,
    ExtensionList,
    ExtensionTypes,
    Ordinal,
    Inject,
    Service,
    extensionPoints,
    extensions,
} from './Extensions';

export const ExtensionRenderer = require('./ExtensionRendererJS');
