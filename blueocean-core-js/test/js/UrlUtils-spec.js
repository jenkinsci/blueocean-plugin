import { assert } from 'chai';

import * as UrlUtils from '../../src/js/utils/UrlUtils';

describe('UrlUtils', () => {
    // TODO: Write some quick tests for anything left in URLUtils
    it('needs a test', () => {
        // <brockman> This oughtta hold those SOBs </brockman>
    });
});
